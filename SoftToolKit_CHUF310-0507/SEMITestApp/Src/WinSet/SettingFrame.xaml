<Controls:MetroWindow x:Class="SEMITestApp.Src.WinSet.SettingFrame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SEMITestApp.Src.WinSet"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"    
        WindowStartupLocation="CenterOwner"
        BorderThickness="1"
        BorderBrush="{DynamicResource AccentColorBrush}"
        Title="Setting" Height="650" Width="1030">
    
    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>
            <Controls:Flyout Header="Maintenance Stations" Position="Left"  Theme="Accent">
                <Grid Margin="10" Width="435">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="5*"></RowDefinition>
                        <RowDefinition Height="1*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <ListBox x:Name="lbStations" Grid.Row="0" ItemsSource ="{Binding WorkStationList}" SelectedIndex="{Binding StationIndex}" SelectedValue="{Binding ElementName=txtStation, Path=Text, Mode=OneWayToSource}"></ListBox>
                    <StackPanel Grid.Row="1" Height="40"  Orientation="Horizontal">
                        <TextBox x:Name="txtStation" Margin="{StaticResource ItemMargin}" Controls:TextBoxHelper.Watermark="text can't start with number" Width="150" Text="{Binding FlyoutStation, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                        <Button Content="Add" Width="90" Margin="{StaticResource ItemMargin}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="add-station-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <Button Content="Edit" Width="90" Margin="{StaticResource ItemMargin}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="edit-station-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <Button Content="Delete" Width="90" Margin="{StaticResource ItemMargin}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="delete-station-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </StackPanel>
                </Grid>
            </Controls:Flyout>
        </Controls:FlyoutsControl>
    </Controls:MetroWindow.Flyouts>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TreeView x:Name="treeSets" Grid.Column="0" Grid.RowSpan="2" BorderBrush="{DynamicResource AccentColorBrush}" BorderThickness="1"  ItemsSource="{Binding SetLists}" SelectedItemChanged="treeSets_SelectedItemChanged">
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource MetroTreeViewItem}">
                    <Setter Property="IsExpanded" Value="True" />
                    <Setter Property="IsSelected" Value="{Binding Selected, Mode=TwoWay}"></Setter>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding ItemNodes}">
                    <TextBlock VerticalAlignment="Center" FontSize="14" Text="{Binding ItemName}"  Margin="2,0,0,0">
                    </TextBlock>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        <Frame Grid.Row="0" Grid.Column="1" x:Name="NaviFrame" NavigationUIVisibility="Hidden" Margin="3 0 3 0"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="NaviBackButton"
                    DockPanel.Dock="Left"
                    Style="{DynamicResource MetroCircleButtonStyle}"
                    Height="40"
                    Width="50"
                    FontFamily="Segoe UI Symbol"
                    FontSize="16"
                    VerticalAlignment="Bottom">
                <Rectangle Width="20"
                           Height="15">
                    <Rectangle.Fill>
                        <VisualBrush Stretch="Fill">
                            <VisualBrush.Visual>
                                <Canvas Width="48"
                                        Height="48"
                                        Clip="F1 M 0,0L 48,0L 48,48L 0,48L 0,0"
                                        UseLayoutRounding="False">
                                    <Path Width="25"
                                          Height="18"
                                          Canvas.Left="12"
                                          Canvas.Top="15"
                                          Stretch="Fill"
                                          Fill="{DynamicResource BlackBrush}"
                                          Data="F1 M 12,22L 12,26L 28.25,26L 21,33L 27.5,33L 37,24L 27.5,15L 21,15L 28.25,22L 12,22 Z " />
                                </Canvas>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <Button.LayoutTransform>
                    <ScaleTransform ScaleX="-1" />
                </Button.LayoutTransform>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="setting-prev"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Button x:Name="NaviForwardButton"
                    DockPanel.Dock="Right"
                    Style="{DynamicResource MetroCircleButtonStyle}"
                    Height="40"
                    Width="50"
                    FontFamily="Segoe UI Symbol"
                    FontSize="16"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    IsEnabled="True">
                <Rectangle Width="20"
                           Height="15">
                    <Rectangle.Fill>
                        <VisualBrush Stretch="Fill">
                            <VisualBrush.Visual>
                                <Canvas Width="48"
                                        Height="48"
                                        Clip="F1 M 0,0L 48,0L 48,48L 0,48L 0,0"
                                        UseLayoutRounding="False">
                                    <Path Width="25"
                                          Height="18"
                                          Canvas.Left="12"
                                          Canvas.Top="15"
                                          Stretch="Fill"
                                          Fill="{DynamicResource BlackBrush}"
                                          Data="F1 M 12,22L 12,26L 28.25,26L 21,33L 27.5,33L 37,24L 27.5,15L 21,15L 28.25,22L 12,22 Z " />
                                </Canvas>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="setting-next"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Label FontSize="25" x:Name="NaviTitle" Margin="0 5 0 0" Width="80"/>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
