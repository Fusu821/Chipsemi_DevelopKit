<UserControl x:Class="SEMIDevelopKit.Src.WinSub.TouchFrame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:avalonDock="http://schemas.xceed.com/wpf/xaml/avalondock"
             xmlns:wpfCtrl="clr-namespace:SEMIDevelopKit.Src.Util"
             xmlns:wndSub="clr-namespace:SEMIDevelopKit.Src.WinSub"
             xmlns:app_util="clr-namespace:SEMIDevelopKit.Src.Util"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Background="Gray">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="180"></RowDefinition>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0">
            <wpfCtrl:WpfDisplayView x:Name="dispTouch" Touch="{Binding TouchData}" Width="{Binding DisplayWidth}"></wpfCtrl:WpfDisplayView>
        </ContentControl>

        <GridSplitter Height="2" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"></GridSplitter>

        <avalonDock:DockingManager  Grid.Row="1" x:Name="dockManager"  AllowMixedOrientation="True">
            <avalonDock:DockingManager.Theme>
                <avalonDock:VS2010Theme></avalonDock:VS2010Theme>
            </avalonDock:DockingManager.Theme>

            <avalonDock:LayoutRoot>
                <avalonDock:LayoutPanel Orientation="Horizontal">
                    <avalonDock:LayoutAnchorablePaneGroup>
                        <avalonDock:LayoutAnchorablePane x:Name="panelLeft">
                            <avalonDock:LayoutAnchorable Title="Param">
                                <Grid Background="#AAAAAA">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="210"></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <ScrollViewer VerticalScrollBarVisibility="Visible" Grid.Column="0">
                                        <UniformGrid Rows="9" Columns="2" Height="220">
                                            <CheckBox Content="ClearScreen" IsChecked="{Binding ClearScreen}" Margin="2"></CheckBox>
                                            <CheckBox Content="Coordinate" IsChecked="{Binding ShowCoordinate}" Margin="2"></CheckBox>
                                            <CheckBox Content="Once" IsChecked="{Binding GraphOnce}" Margin="2"></CheckBox>
                                            <Control></Control>
                                            <!--<CheckBox Content="HideTickRate" IsChecked="{Binding HideTickRate}" Margin="2"></CheckBox>-->
                                            <CheckBox Content="ShowPackets" IsChecked="{Binding ShowPackets}" Margin="2"></CheckBox>
                                            <CheckBox Content="ShowHandStyle" IsChecked="{Binding ShowHandStyleData}" Margin="2"></CheckBox>
                                            <app_util:WpfToggleButton Width="35" Height="15" Margin="2" IsChecked="{Binding CatchChecked}">
                                                <ToggleButton.Style>
                                                    <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource WpfToggleButtonStyle}">
                                                        <Setter Property="Content" Value="ACD"></Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter Property="Content" Value="SCD"></Setter>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ToggleButton.Style>
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Checked">
                                                        <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="touch-catch-checked"/>
                                                    </i:EventTrigger>
                                                    <i:EventTrigger EventName="Unchecked">
                                                        <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="touch-stopcatch-unchecked"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </app_util:WpfToggleButton>
                                            <Control></Control>
                                            <Label Content="GraghMode"></Label>
                                            <ComboBox SelectedIndex="{Binding GraghMode}" Margin="2">
                                                <ComboBoxItem Content="Points"></ComboBoxItem>
                                                <ComboBoxItem Content="Lines"></ComboBoxItem>
                                            </ComboBox>
                                            <Label Content="Resolution-X" Margin="1"></Label>
                                            <TextBox Text="{Binding ResolutionX}" Margin="2"></TextBox>
                                            <Label Content="Resolution-Y" Margin="1" Height="25"></Label>
                                            <TextBox Text="{Binding ResolutionY}" Margin="2"></TextBox>
                                            <Label Content="Lines-X" Margin="1" Height="25"></Label>
                                            <TextBox Text="{Binding LinesX}" Margin="2"></TextBox>
                                            <Label Content="Lines-Y" Margin="1"></Label>
                                            <TextBox Text="{Binding LinesY}" Margin="2"></TextBox>
                                            <TextBlock Text="Accuracy:" Margin="2"></TextBlock>
                                            <ComboBox ItemsSource="{Binding ListAccuracy}" DisplayMemberPath="TypeName" SelectedValue="{Binding Accuracy}" SelectedValuePath="TypeVal" Margin="2"></ComboBox>
                                        </UniformGrid>
                                    </ScrollViewer>
                                    
                                    <GridSplitter Width="2"></GridSplitter>
                                    <!--TextBlock x:Name="textShow" Grid.Row="0" Grid.Column="1" Text="{Binding TextInfo}"></TextBlock-->
                                    <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Grid.Column="1">
                                        <ItemsControl  ItemsSource ="{Binding ListPacket}" MinWidth="400" VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" Background="#FEFADA">
                                            <ItemsControl.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="Clear" Click="Clear_Click"></MenuItem>
                                                    <MenuItem Header="Save Log" Click="SaveLog_Click">
                                                    </MenuItem>
                                                    <MenuItem Header="Type:">
                                                        <MenuItem Header="Points" Click="SetPoint_Click"/>
                                                        <MenuItem Header="Data Packet" Click="SetDataPacket_Click"/>
                                                    </MenuItem>
                                                </ContextMenu>
                                            </ItemsControl.ContextMenu>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </avalonDock:LayoutAnchorable>
                            <avalonDock:LayoutAnchorable Title="Others">
                            </avalonDock:LayoutAnchorable>
                        </avalonDock:LayoutAnchorablePane>
                    </avalonDock:LayoutAnchorablePaneGroup>
                </avalonDock:LayoutPanel>
            </avalonDock:LayoutRoot>
        </avalonDock:DockingManager>
    </Grid>
</UserControl>
