<UserControl x:Class="SEMIDevelopKit.Src.WinSub.RawDataFrame570"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:avalonDock="http://schemas.xceed.com/wpf/xaml/avalondock"
             xmlns:app_util="clr-namespace:SEMIDevelopKit.Src.Util"
             xmlns:local="clr-namespace:SEMIDevelopKit.Src.WinSub"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="170"></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <!--<app_util:WpfDataGrid Grid.Row="0" MatrixSource="{Binding RawDataMatrix, Mode=TwoWay}" Reference="{Binding ReferenceData, Mode=TwoWay}"></app_util:WpfDataGrid>-->
            <app_util:WpfRawDataView Grid.Row="0" x:Name="dispRawData" Reference="{Binding ReferenceData, Mode=TwoWay}"></app_util:WpfRawDataView>
        </ScrollViewer>

        <GridSplitter Height="2" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"></GridSplitter>
        <avalonDock:DockingManager  Grid.Row="1" x:Name="dockManager"  AllowMixedOrientation="True">
            <avalonDock:DockingManager.Theme>
                <avalonDock:VS2010Theme></avalonDock:VS2010Theme>
            </avalonDock:DockingManager.Theme>

            <avalonDock:LayoutRoot>
                <avalonDock:LayoutPanel Orientation="Horizontal">
                    <avalonDock:LayoutAnchorablePaneGroup>
                        <avalonDock:LayoutAnchorablePane x:Name="panelLeft">
                            <avalonDock:LayoutAnchorable Title="Main" CanAutoHide="False" CanHide="False" CanClose="False">
                                <Grid Background="#AAAAAA">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" ></ColumnDefinition>
                                        <ColumnDefinition Width="180"></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition ></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <app_util:WpfToggleButton Grid.Row="0" Grid.Column="0" Width="150" Height="60" IsChecked="{Binding CatchChecked}">
                                        <ToggleButton.Style>
                                            <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource WpfToggleButtonStyle}">
                                                <Setter Property="Content" Value="Catch-Data"></Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Content" Value="Stop-Catch"></Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ToggleButton.Style>
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Checked">
                                                <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-catch-checked"/>
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="Unchecked">
                                                <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-stopcatch-unchecked"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </app_util:WpfToggleButton>
                                    <!--GroupBox Header="DataType" Grid.Column="1" Style="{x:Null}" BorderThickness="0.3" Margin="1">
                                    </GroupBox>
                                    <GroupBox Header="Debug" Grid.Column="2" Style="{x:Null}" BorderThickness="0.3" Margin="1">
                                        <StackPanel Orientation="Vertical">
                                           
                                        </StackPanel>
                                    </GroupBox-->
                                </Grid>
                            </avalonDock:LayoutAnchorable>
                            <avalonDock:LayoutAnchorable Title="Analyze" CanAutoHide="False" CanHide="False" CanClose="False">
                                <Grid Background="#AAAAAA">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="40"></RowDefinition>
                                        <RowDefinition></RowDefinition>
                                        <!--<RowDefinition Height="45"></RowDefinition>-->
                                    </Grid.RowDefinitions>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="StcType:" Margin="5, 2"></TextBlock>
                                        <ComboBox ItemsSource="{Binding ListStcType}" DisplayMemberPath="TypeName" SelectedValue="{Binding StcType}" SelectedValuePath="TypeVal" Margin="5, 2" Width="100"></ComboBox>
                                        <TextBlock Text="Frames:" Margin="5, 2"></TextBlock>
                                        <Slider x:Name="stcSlider" Maximum="1000" Value="{Binding StcFrames}" Orientation="Horizontal" Width="200" Margin="5, 5"></Slider>
                                        <TextBlock Text="{Binding ElementName=stcSlider,Path=Value}" Margin="5, 2"></TextBlock>
                                        <TextBlock Text="SNRType:" Margin="10, 2" Visibility="{Binding StcType, Converter={StaticResource EnumerToVisibleConverter}, ConverterParameter=5}" ></TextBlock>
                                        <app_util:WpfToggleButton  VerticalAlignment="Center" Width="150" Height="40" IsChecked="{Binding CatchAnalyzeChecked}">
                                            <ToggleButton.Style>
                                                <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource WpfToggleButtonStyle}">
                                                    <Setter Property="Content" Value="Catch-Data"></Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="Content" Value="Stop-Catch"></Setter>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ToggleButton.Style>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Checked">
                                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-catch-analyze-checked"/>
                                                </i:EventTrigger>
                                                <i:EventTrigger EventName="Unchecked">
                                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-stopcatch-analyze-unchecked"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </app_util:WpfToggleButton>
                                        <ComboBox ItemsSource="{Binding ListSNRType}" DisplayMemberPath="TypeName" SelectedValue="{Binding SNRType}" SelectedValuePath="TypeVal" Margin="5, 2" Width="150" Visibility="{Binding StcType, Converter={StaticResource EnumerToVisibleConverter}, ConverterParameter=5}"></ComboBox>
                                    </StackPanel>
                                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                        <!--<app_util:WpfDataGrid MatrixSource="{Binding StatisticMatrix, Mode=TwoWay}" Reference="100"></app_util:WpfDataGrid>-->
                                        <app_util:WpfRawDataView Grid.Row="0" x:Name="dispStatisticRawData" Reference="100" ></app_util:WpfRawDataView>
                                    </ScrollViewer>
                                    <!--<app_util:WpfToggleButton  Grid.Row="2" Grid.Column="0"  Width="150" Height="40" IsChecked="{Binding CatchChecked}">
                                        <ToggleButton.Style>
                                            <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource WpfToggleButtonStyle}">
                                                <Setter Property="Content" Value="Catch-Data"></Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Content" Value="Stop-Catch"></Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ToggleButton.Style>
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Checked">
                                                <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-catch-checked"/>
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="Unchecked">
                                                <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-stopcatch-unchecked"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </app_util:WpfToggleButton>-->
                                </Grid>
                            </avalonDock:LayoutAnchorable>
                            <avalonDock:LayoutAnchorable Title="Simulation" CanAutoHide="False" CanHide="False" CanClose="False">
                                <Grid Background="#AAAAAA">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="25"></RowDefinition>
                                        <RowDefinition Height="25"></RowDefinition>
                                        <RowDefinition></RowDefinition>
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"></ColumnDefinition>
                                            <ColumnDefinition></ColumnDefinition>
                                            <ColumnDefinition Width="80"></ColumnDefinition>
                                            <ColumnDefinition Width="100"></ColumnDefinition>
                                            <ColumnDefinition Width="50"></ColumnDefinition>
                                            <ColumnDefinition Width="50"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Button Content="FilePath" Margin="5, 1" Grid.Column="0">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="simulation-path-click"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                        <TextBox Text="{Binding SimulationPath}" Margin="5, 2" Grid.Column="1"></TextBox>
                                        <CheckBox Content="Simulate" IsChecked="{Binding SimulationChecked}" Margin="5, 4" Grid.Column="2"></CheckBox>
                                        <Slider x:Name="simulateSpeed" Maximum="1000" Value="{Binding SimulationSpeed}" Orientation="Horizontal" Margin="5, 3" Grid.Column="3"></Slider>
                                        <TextBlock Text="{Binding ElementName=simulateSpeed,Path=Value}" Margin="5, 3" Grid.Column="4"></TextBlock>
                                        <TextBlock Text="{Binding SimulationPosition}" Margin="5, 3" Grid.Column="5"></TextBlock>
                                    </Grid>
                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"></ColumnDefinition>
                                            <ColumnDefinition ></ColumnDefinition>
                                            <ColumnDefinition Width="50"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Button Content="Frame-Pause" Margin="5,1" Grid.Column="0">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="simulation-next-click"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                        <Slider x:Name="stopFrame" Value="{Binding SimulationStop}" Maximum="{Binding SimulationMax}"  Orientation="Horizontal" Margin="5, 3" Grid.Column="1"></Slider>
                                        <TextBlock Text="{Binding ElementName=stopFrame,Path=Value}" Grid.Column="2" Margin="5, 3"></TextBlock>
                                    </Grid>
                                </Grid>
                            </avalonDock:LayoutAnchorable>
                        </avalonDock:LayoutAnchorablePane>
                    </avalonDock:LayoutAnchorablePaneGroup>
                </avalonDock:LayoutPanel>
            </avalonDock:LayoutRoot>
        </avalonDock:DockingManager>

    </Grid>
</UserControl>
