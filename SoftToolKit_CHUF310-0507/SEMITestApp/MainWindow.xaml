<Controls:MetroWindow x:Class="SEMITestApp.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SEMITestApp"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"              
        mc:Ignorable="d"
        WindowTitleBrush="Gray"
        BorderThickness="1"
        Icon="Image\SemiChip.png"
        BorderBrush="{DynamicResource AccentColorBrush}"  
        xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
        Dialog:DialogParticipation.Register="{Binding}"
        Title="{Binding SoftWareVersion}" Height="650" Width="1100" Closing="MetroWindow_Closing">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro.Resources;component/Icons.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/FlatSlider.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Ellipse x:Key="AppThemeMenuIcon"
                     x:Shared="False"
                     Width="16"
                     Height="16"
                     StrokeThickness="1"
                     Stroke="{Binding BorderColorBrush, Mode=OneWay}"
                     Fill="{Binding ColorBrush, Mode=OneWay}">
            </Ellipse>
            <Style x:Key="AppThemeMenuItemStyle"
                   TargetType="{x:Type MenuItem}"
                   BasedOn="{StaticResource MetroMenuItem}">
                <Setter Property="Command"
                        Value="{Binding ChangeAccentCommand}" />
                <Setter Property="Icon"
                        Value="{StaticResource AppThemeMenuIcon}" />
                <Setter Property="Header"
                        Value="{Binding Name, Mode=OneWay}" />
            </Style>

            <!--Dialog:CustomDialog x:Key="OnLineLoginDialog"
                                 Title="OnLine-Login"
                                 x:Name="OnLineLoginDialog">
                <UniformGrid Rows="4" Columns="2" x:Name="onlineUnit">
                    <TextBlock Text="LineID" Margin="5" HorizontalAlignment="Right"></TextBlock>
                    <TextBox Name="lineID" Text="M1TPST0100}" Margin="5"></TextBox>
                    <TextBlock Text="Account" Margin="5" HorizontalAlignment="Right"></TextBlock>
                    <TextBox Name="account" Text="C99999" Margin="5"></TextBox>
                    <TextBlock Text="Password" Margin="5" HorizontalAlignment="Right"></TextBlock>
                    <TextBox Name="password" Text="123456" Margin="5"></TextBox>
                    <Button Content="Confirm" x:Name="onlineLogin" Margin="30, 5"></Button>
                    <Button Content="Cancel" x:Name="onlineCancel" Margin="30, 5"></Button>
                </UniformGrid>
            </Dialog:CustomDialog-->
            <Dialog:CustomDialog x:Key="DialogMessage"
                                 Title="Warning"
                                 x:Name="DialogMessage">
                <TextBlock Height="200" x:Name="MessageTextBlock" Text="{Binding TipMessage}" Foreground="{DynamicResource AccentColorBrush}" />
            </Dialog:CustomDialog>
        </ResourceDictionary>
    </Window.Resources>


    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>
            <Controls:Flyout Header="SN Scanning:"  Template="{StaticResource FlyoutTemplateEx}" Theme="Accent">
                <StackPanel Orientation="Vertical">
                    <TextBlock Grid.Row="1" TextAlignment="Right" Text="{Binding ProjectInfo}"  FontWeight="SemiBold" FontSize="15" Margin="10, 0, 50, 8"></TextBlock>
                    <ListBox Name="lbxSNCode">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <!--VirtualizingStackPanel Orientation="Horizontal"></VirtualizingStackPanel-->
                                <UniformGrid Rows="1"></UniformGrid>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </StackPanel>
            </Controls:Flyout>
        </Controls:FlyoutsControl>
        
    </Controls:MetroWindow.Flyouts>
    <Controls:MetroContentControl>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <Menu Background="{x:Null}" Grid.Row="0"
                  Margin="5"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Left">
                    <MenuItem Header="Mode" Background="{x:Null}">
                        <MenuItem Header="Single TP Test" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-sigletp-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="Number 2" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-number2tp-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="Number 3" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-number3tp-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="Number 4" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-number4tp-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="Number 5" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-number5tp-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="Number 6" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-number6tp-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="Number 7" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-number7tp-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="Number 8" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-number8tp-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                    </MenuItem>
                    <MenuItem Header="login" Background="{x:Null}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-login-click"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </MenuItem>
                    <MenuItem Header="Theme" ItemsSource="{Binding AppThemes, Mode=OneWay}" ItemContainerStyle="{StaticResource AppThemeMenuItemStyle}" Background="{x:Null}">
                    </MenuItem>
                    <MenuItem Header="Custom" Background="{x:Null}">
                        <MenuItem Header="SN-Input" Background="{x:Null}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="main-flayout-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="{Binding LineText}" Background="{x:Null}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="online-line-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                        <MenuItem Header="{Binding SignText}" Background="{x:Null}" Visibility="{Binding SignVisible}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ctrlClickCommand}" CommandParameter="online-sign-click"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                    </MenuItem>
                </Menu>
                <TextBlock DockPanel.Dock="Right" TextAlignment="Right" Text="{Binding ProjectInfo}" Foreground="Blue" FontWeight="SemiBold" FontSize="15" Margin="10, 8, 50, 0"></TextBlock>
            </DockPanel>
            

            <Frame x:Name="contentFrame" Grid.Row="1" NavigationUIVisibility="Hidden" Source="{Binding FrameContent}"></Frame>
            <StatusBar Grid.Row="2">
                <StatusBarItem Foreground="Yellow"  Content="{Binding DeviceStates, Converter={StaticResource StatusArrayToTextConverter}}"></StatusBarItem>
                <Separator Style="{StaticResource MetroStatusBarSeparator}" />
                <StatusBarItem Foreground="{Binding OnLineStateForground}"  Content="{Binding OnLineState}"></StatusBarItem>
                <Separator Style="{StaticResource MetroStatusBarSeparator}" />
                <StatusBarItem Foreground="White"  Content="{Binding StatisticText}" HorizontalContentAlignment="Right"></StatusBarItem>
                <!--StatusBarItem Content="{Binding ElementName=MainTabControl, Path=SelectedItem.Header, Mode=OneWay}" /-->
            </StatusBar>
        </Grid>
    </Controls:MetroContentControl>
</Controls:MetroWindow>
